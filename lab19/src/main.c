#include "list.h"
#include "data.h"
#include "ui.h"
// #include "ui.h"

int main()
{
    // struct AgenciesList agencies_list;

    // init_agencies_list(&agencies_list);

    // struct Agency new_agency = {true, "Agency Name 1", 3, {"Surname1", "Name1", "Email@mail.com"}, Kharkiv};

    // add_element_to_list_end(&agencies_list, &new_agency);

    // new_agency.name = "Agency Name 2";
    // new_agency.director.surname = "Surname2";
    // new_agency.years_on_market = 1;


    // add_element_to_list_end(&agencies_list, &new_agency);

    // new_agency.name = "Agency Name 3";
    // new_agency.years_on_market = 2;
    // add_element_to_list_end(&agencies_list, &new_agency);
    
    // // print_agencies_list_on_screen(&agencies_list);
    // // putc('\n', stdout);

    // struct AgenciesListElement *searched = NULL;

    // get_element_with_city_from_index(&agencies_list, 3, Kharkiv, &searched);

    // print_agency(&searched->data, stdout);
    // putc('\n', stdout);

    // print_agencies_list_on_screen(&agencies_list);

    // free_agencies_list(&agencies_list);
    start();

    return 0;
}